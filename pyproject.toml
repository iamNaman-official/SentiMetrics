[tool.ruff]
line-length = 250
exclude = [
    ".git",
    ".venv",
    "env",
    "migrations",
    "__pycache__",
]

[tool.ruff.lint]
# E: pycodestyle errors, F: Pyflakes, I: isort (sorts imports automatically)
select = ["E", "F", "I"]
ignore = []