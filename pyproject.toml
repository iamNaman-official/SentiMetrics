[tool.ruff]
line-length = 100
exclude = [
    ".git",
    ".venv",
    "env",
    "migrations",
    "__pycache__",
]

[tool.ruff.lint]
# E: pycodestyle errors, F: Pyflakes, I: isort (sorts imports automatically)
select = ["E", "F", "I"]
ignore = []